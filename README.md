# gen-link

Cross-flake aspect federation over origin-labeled subgraphs. A Class-B conductor:
owns the origin coordinate, the disjoint-union-with-relabel, and the resolution
manifest; delegates every computation to a gen sibling. See Task 12 / REFERENCE.md.
